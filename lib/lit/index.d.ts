/**
  * @property { unknown } viewToRender
  * @property { ViewRoute } route
  * @property { SuuntaTarget } renderTarget
  * */
export function litRenderer(viewToRender: unknown, route: ViewRoute, renderTarget: SuuntaTarget): void;
